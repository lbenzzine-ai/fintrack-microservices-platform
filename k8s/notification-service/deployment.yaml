apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: fintrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: 692101471621.dkr.ecr.us-east-1.amazonaws.com/fintrack-notification-service:23252e15e260db78d01f61f4459685c59898e66f
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: fintrack-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fintrack-secrets
                  key: db-password
          resources:
            requests:
              cpu: "256m"
              memory: "512Mi"
            limits:
              cpu: "512m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: fintrack
spec:
  selector:
    app: notification-service
  ports:
    - port: 8084
      targetPort: 8084
