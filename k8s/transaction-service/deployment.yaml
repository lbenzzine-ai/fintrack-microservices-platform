apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  namespace: fintrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      labels:
        app: transaction-service
    spec:
      containers:
        - name: transaction-service
          image: 692101471621.dkr.ecr.us-east-1.amazonaws.com/fintrack-transaction-service:23252e15e260db78d01f61f4459685c59898e66f
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: fintrack-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fintrack-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: fintrack-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: "256m"
              memory: "512Mi"
            limits:
              cpu: "512m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: fintrack
spec:
  selector:
    app: transaction-service
  ports:
    - port: 8083
      targetPort: 8083
